package A_TypesAndClasses

// Types - how to group values that have similar properties into categories.

// A type is a name for a collection related values.

// Type of boolean denotes the set of values true and false

// Type error - applying the function to one or more arguments of the wrong type, which cannot operate on each other
// Statically typed languages can catch these, at compile time, where dynamic at runtime.

// A well formed expression has a type calculated at compile time via a process called type inference.

// Kotlin basic types: numbers, characters, booleans, arrays, strings,

// List - sequence of values of the same type

// Tuple - sequence of values of different type